# AI-DLC Workflows (v2)

> **Humans codify the judgement. AI orchestrates and self-verifies — deterministically.**

> [!NOTE]
> v2 implements the autonomous software development vision, the core principles and the architecture as specified in [AI-DLC Workflows 2.0 Specification](./assets/AI-DLC-Workflows-2.0-Specification.pdf) whitepaper. v2 is an active development branch of [aidlc-workflows](https://github.com/awslabs/aidlc-workflows). The layout, skill set, and install model are still evolving — expect breaking changes. For the stable release, use `main`.

> [!IMPORTANT]
> Generative AI can make mistakes. You should consider reviewing all output and costs generated by your chosen AI model and agentic coding assistant. See [AWS Responsible AI Policy](https://aws.amazon.com/ai/responsible-ai/policy/).

For learning more about AI-DLC, read this [blog](https://aws.amazon.com/blogs/devops/ai-driven-development-life-cycle/) and the [Method Definition Paper](https://prod.d13rzhkk8cj2z0.amplifyapp.com/) referred in it.


## Table of Contents

- [Getting Started](#getting-started)
- [Platform Support](#platform-support)
- [Usage](#usage)
- [Troubleshooting](#troubleshooting)
- [Contributing](#contributing)
- [License](#license)

---

## Getting Started

v2 does not yet ship release zips. To try it, clone this branch and follow the platform-specific build and install instructions for your coding agent.

### Prerequisites

- [Node.js](https://nodejs.org/) — used to run the build script and the deterministic process checker
- One of the [supported platforms](#platform-support)

### Clone the repo

Clone into a folder **outside** your project (e.g. `~/workspaces/aidlc-workflows`). The commands assume you cloned this repo to `~/workspaces/aidlc-workflows`. Replace the path if you cloned elsewhere.

```bash
git clone -b v2 https://github.com/awslabs/aidlc-workflows.git ~/workspaces/aidlc-workflows
cd ~/workspaces/aidlc-workflows
```

Build the AI-DLC distribution:

```bash
npm run build
```

This produces `dist/kiro-ide/.kiro/`.

Then jump to the setup instructions for your platform below.

---

## Platform Support

v2 currently supports:

- [Kiro IDE](https://kiro.dev/)
- [Kiro CLI](https://kiro.dev/cli/)

More targets (Q Developer, Cursor, Cline, Claude Code, Copilot, etc.) are planned — see v1 if you need those today.

### Kiro

Kiro uses a `.kiro/` directory at your project root for workspace metadata (steering, hooks, specs, settings, etc.). AI-DLC v2 plugs into that same directory by adding six subdirectories: `agents/`, `conventions/`, `hooks/`, `skills/`, `stages/`, and `tools/`.

- If `.kiro/` already exists, the install commands below merge into it. Anything you already have under `.kiro/` is preserved.
- If it doesn't, the commands create it.

#### Install

Run these commands from your **project root** (the folder you open in Kiro):

**macOS / Linux:**
```bash
mkdir -p .kiro
cp -R ~/workspaces/aidlc-workflows/dist/kiro-ide/.kiro/ .kiro/
```

**Windows (PowerShell):**
```powershell
New-Item -ItemType Directory -Force -Path ".kiro"
Copy-Item -Recurse -Force "$env:USERPROFILE\workspaces\aidlc-workflows\dist\kiro-ide\.kiro\*" ".kiro\"
```

**Windows (CMD):**
```cmd
if not exist .kiro mkdir .kiro
xcopy %USERPROFILE%\workspaces\aidlc-workflows\dist\kiro-ide\.kiro .kiro\ /E /I /Y
```

After install, your `.kiro/` directory should include these subdirectories (alongside anything else Kiro already manages there):

```text
<project-root>/
└── .kiro/
    ├── agents/
    ├── conventions/
    ├── hooks/
    ├── skills/
    ├── stages/
    └── tools/
```

#### Verify

Open the skills panel in Kiro IDE and confirm you see entries for the AI-DLC skills (`aidlc-orchestration`, `aidlc-kickoff`, `aidlc-requirements-analysis-skill`, etc.) under your workspace.

Use Kiro IDE in **Vibe** mode to run the AI-DLC workflow.

---

## Usage

1. Start any software development task by stating your intent in chat.
2. The AI-DLC orchestrator activates automatically and proposes a workflow tailored to your intent.
3. Answer the structured questions it generates and approve the proposed plan.
4. Review each artifact at every stage and approve before the next stage runs.
5. Artifacts land in `org-ai-kb/aidlc-docs/intent-<nnn>-<slug>/` next to your project.

---

## Troubleshooting

| Problem | Solution |
| --- | --- |
| Skills not loading in Kiro | Confirm `.kiro/` at your project root contains `agents/`, `skills/`, `hooks/`, `stages/`, `conventions/`, and `tools/`. Restart your Kiro session after copying. |
| Build fails | Confirm Node.js is installed and on your `PATH`. Run `npm run clean && npm run build`. |
| File encoding issues | Ensure files are UTF-8 encoded. |
| Rules not applied in session | Start a new chat session after updating `.kiro/`. |
| Windows paths not resolving | Use forward slashes `/` inside markdown files. Backslashes can break path resolution. |

---

## Contributing

This branch is pre-release. If you want to experiment:

- Edit `src/` only — never hand-edit `dist/`, it is generated.
- Rebuild with `npm run build` after changes.
- Stages are self-contained folders — add a new stage by creating `src/stages/<name>/definition.md` + `templates/`.
- Personas are YAML — add a new persona by creating `src/personas/<name>.yaml`.
- Skills follow the frontmatter + markdown format — add under `src/skills/<name>/SKILL.md`.

---

## License

This library is licensed under the MIT-0 License. See the [LICENSE](LICENSE) file.
